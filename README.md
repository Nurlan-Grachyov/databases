## Проект: parser_data

Простое решение для работы с базами данных.

### Добавлено приложение для получения операций, подходящих по фильтрам

Это приложение предоставляет API для получения данных о торговых операциях и динамике рынка с возможностью фильтрации и
ограничения объема данных. В основном используются FastAPI и асинхронное взаимодействие с базой данных и Redis для
кэширования.

#### Основные эндпоинты

1. **Получение последних дат торгов (/last_dates)**
   __Описание:__ Возвращает список последних уникальных дат торгов.<br>
   __Параметры:__<br>
   limit_days (int): Количество последних дат, которые нужно получить (по умолчанию 10).<br>
   __Работа с данными:__<br>
   Если дата после 14:11, данные извлекаются из базы данных и сохраняются в Redis. В противном случае — из Redis.
2. **Получение динамики данных (/get_dynamics)**
   __Описание__: Получает динамику торговых данных за заданный диапазон дат с возможностью фильтрации по виду нефти,
   типу поставки и базе доставки.<br>
   __Параметры__:<br>
   start_date (date): Начальная дата диапазона.<br>
   end_date (date): Конечная дата диапазона.<br>
   oil_id (int, опционально): ID вида нефти.<br>
   delivery_type_id (int, опционально): ID типа поставки.<br>
   delivery_basis_id (int, опционально): ID базы доставки.<br>
   __Работа с данными:__<br>
   В зависимости от времени (после или до 14:11) данные либо извлекаются из базы данных и кэшируются в Redis, либо из
   Redis.
3. **Получение последних операций трейдинга (/get_trading_results)**
   __Описание:__ Возвращает список последних торговых операций с фильтрацией и ограничением по количеству.<br>
   __Параметры:__<br>
   __limit_trades (int):_ Максимальное число операций (по умолчанию 10).<br>
   __oil_id (int, опционально):__ ID вида нефти.<br>
   __delivery_type_id (int, опционально):__ ID типа поставки.<br>
   __delivery_basis_id (int, опционально):__ ID базы доставки.<br>
   __Особенности:__
   Результаты пересохраняются в Redis, если текущий момент после 14:11, что позволяет ускорить повторные запросы.<br>

### Технические детали

**Используемые технологии:**<br>
FastAPI — для построения REST API.<br>
SQLAlchemy — для взаимодействия с базой данных.<br>
Redis — для кэширования.<br>
Асинхронное программирование — для повышения производительности.<br>
**Работа с Redis:**<br>
Для хранения и быстрого доступа к последним данным используется Redis. Время после 14:11 служит триггером для
пересоздания кеша.<br>
Обработка дат:<br>
Для гибкого парсинга дат используется функция parse_flexible_date, что позволяет принимать разные форматы дат.<br>
**Как запустить**<br>
Убедитесь, что у вас установлены все зависимости.<br>
Запустите Redis-сервер.<br>
Настройте подключение к базе данных.<br>
Запустите FastAPI приложение.<br>

### ⚡ Поддержка асинхронного скачивания

Проект поддерживает не только синхронные операции скачивания, но и асинхронное скачивание с использованием `asyncio`.
Это позволяет эффективно управлять большим количеством запросов и значительно ускоряет процесс загрузки данных.

Вы можете использовать асинхронные функции для скачивания файлов, что особенно полезно при обработке больших объемов
данных или параллельных задачах.

⚙️ Установка зависимостей

Данный проект управляет зависимостями через Poetry.

Установите все нужные зависимости с помощью команды:

````
poetry install
````

Это действие загрузит все указанные в конфигурации библиотеки и подготовит виртуальное окружение.

### 🚀 Запуск скачивания файлов с помощью асинхронного инструмента (рекомендуется):

Для запуска проекта выполните команду:

````
python parser/async_download/send_data.py
````

### 🔍 Структура проекта

Файлы и каталоги организованы следующим образом:

```
databases/
└── app/                         
│    ├── main.py                           
│    ├── routers.py/                   
│    ├── utils/               
│
├── parser/
│   │       # Модуль для отправки данных
│   ├── sync/                
│   │   ├── __init__.py
│   │   ├── data_parser.py   
│   │   ├── database.py    
│   │   ├── models.py
│   │   ├── read_data.py
│   │   └── send_data.py
│   └── async/             
│       ├── __init__.py
│       ├── data_parser.py 
│       ├── database.py    
│       ├── models.py
│       ├── read_data.py
│       └── send_data.py
├── data/
│   ├── async_files/         
│   └── sync_files/          
├── dist/
│   ├── databases-0.1.0.tar.gz  
│   └── databases-0.1.0-py3-none-any.whl  
├── logs/
│   ├── parser_errors.log
│   ├── read_files_errors.log
│   └── send_errors.log
└── .env                       
├── README.md 
````

### Обоснование обязательных аргументов в app.routers

**Диапазон дат (start_date, end_date):**<br>
В запросах, связанных с динамикой данных и торговыми операциями, важно ограничить временной диапазон,
чтобы получать актуальную и релевантную информацию.<br>
start_date и end_date задают конкретный период, за который нужно анализировать данные.
Без указания диапазона невозможно определить, какие именно записи нужны, что сделает запрос бессмысленным.

**Ограничение объема данных (параметры limit_days и limit_trades):**<br>
Эти параметры позволяют явно указать, сколько записей или дат нужно получить.
Без этого есть риск вернуть слишком много данных, что может привести к перегрузке сервера или ухудшению
производительности.
Ограничение помогает обеспечить управляемый и быстрый отклик.

### 🛠 Поддерживаемые версии Python

Поддерживаются версии Python начиная с 3.12 включительно.

### 💬 Авторы и контактная информация
Автор проекта:

Nurlan (nurlan.grachyov@mail.ru)
